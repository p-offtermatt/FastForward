\begin{figure}

  \begin{center}
  {%
  \begin{tikzpicture}[scale=\plotscale, every node/.style={scale=\plotscale}, font=\large]%
      \begin{axis}[%
          xmin=-0.5,
          xmax=50.5,
          ymin=-0.5,
          height=5cm,
          log basis y={2},
          ytick={0,0.5,1,2,3,4,5,6,7},
          yticklabels={0,1,2,4,8,16,32,64},
          width=\textwidth,
          xlabel style={align=center},
          xlabel={length of the shortest witness},
          ylabel style={align=center},
          ylabel={difference with \\ exact distance},
          legend style={
            at={(0, 0.65)},
            anchor=west},
          reverse legend,
        ]%
        
        \addplot[mark=none] coordinates {(0,0)(50.5,0)};
        \IfFileExists{data/shortest_path_data.tex}{%
          \input{data/shortest_path_data.tex}%
        }
        
        
      \end{axis}%
    \end{tikzpicture}%
    }% 
    \\
    %% Bottom legend
    \begin{tikzpicture}[very thick, scale=\plotscale,
      every node/.style={scale=\plotscale}]%
    \begin{customlegend}[legend columns=6,
    legend style={draw=none, column sep=1ex},
    legend entries={
\textsc{FF(GBFS, $\mathcal{D}_{\Q}$)}
,
\textsc{LoLA}
, 
\textsc{Bfc}
}]%
    

\addlegendmark{GBFS}{square*}

\addlegendmark{LoLA}{+}

\addlegendmark{BFC}{asterisk}

    \end{customlegend}
    \end{tikzpicture}
  \end{center}
    \caption{Length of the returned witness, per tool, compared to the
      length of a shortest witness. Note that only instances are included for which either \textsc{FF(\astar, $\mathcal{D}_{\Q}$)} or \textsc{FF(Dijkstra)}
      finished, since these tools are guaranteed to find a shortest path. Corresponds to Figure 5 of the paper.}\label{fig:path-comparison}
    
\end{figure}
